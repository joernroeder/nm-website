// Generated by CoffeeScript 1.6.2
"use strict";(function($) {
  var editorTypes;

  editorTypes = ['markdown'];
  return $('[contenteditable="true"][data-editor-type]').each(function(i, el) {
    var $el, api, close, elType, getContent, open, toggle;

    open = function() {
      return api.show();
    };
    close = function() {
      return api.hide();
    };
    toggle = function() {
      if (api.tooltip) {
        return api.toggle();
      } else {
        return open();
      }
    };
    getContent = function() {
      console.log(elType);
      return "<h1>" + elType + " Editor</h1>";
    };
    $el = $(el);
    elType = $el.data('editor-type');
    if (-1 === $.inArray(elType, editorTypes)) {
      return;
    }
    $el.qtip({
      content: {
        text: getContent(),
        title: ''
      },
      position: {
        at: 'right center',
        my: 'left center'
      },
      show: {
        event: false
      },
      hide: {
        event: false,
        fixed: true
      },
      style: 'editor-popover'
    });
    api = $el.qtip('api');
    console.log(api);
    return $el.on('click', function() {
      return toggle();
    });
  });
})(jQuery);
